Audit and fix device fingerprinting: security, bottlenecks, UX gaps

SECURITY FIXES:
- Added isValidFingerprintHash() validator (64-char hex regex) to prevent
  non-hex strings from reaching DB queries in login, signup, and check-device
- Fixed timing oracle in check-device route: when user doesn't exist, now
  runs a dummy DB query to equalize response time (prevents email enumeration)
- Reduced check-device rate limit from 20 to 10 requests/15min to slow
  enumeration attempts
- Login route now uses hex-validated fingerprint before querying trusted_devices

BOTTLENECK FIXES:
- Login trusted-device check: select only id column instead of full row
- Login trusted-device update: fire-and-forget lastUsedAt update (non-blocking)
- Login device trust on password success: replaced check-then-insert (2 queries)
  with single onConflictDoUpdate upsert (1 query) — eliminates race condition
- Signup device trust: same onConflictDoUpdate pattern

UX GAPS FIXED:
- Signup now trusts the device on account creation (was password-only next login)
- Signup page generates and sends fingerprint hash on mount
- Added trusted devices management UI to settings page:
  * Lists all trusted devices with label, creation date, last used
  * Revoke (delete) any trusted device with one tap
  * Uses existing GET/DELETE /api/auth/trusted-devices API
  * Mobile-optimized with 44px touch targets, truncation, responsive layout

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
