Fix prayer button height, goal save-on-blur, goal checkmark size

1. Prayer buttons on calendar day view (schedule tab):
   - Changed from flex-wrap to single-row flex on mobile
   - Reduced vertical padding from py-1.5 to py-1 on mobile
   - Removed gap-0.5 between label and time (tighter)
   - Smaller font sizes on mobile (10px label, 9px time)
   - Smaller checkmark icon on mobile (h-2.5 w-2.5)
   - leading-tight to prevent line-height bloat
   - Buttons no longer wrap to 2 rows — all 6 fit in one row

2. Goals not saving on blur:
   - Added onBlur handler to the edit container in both ListView
     and TreeView
   - When focus leaves the title/description inputs entirely
     (relatedTarget is outside the container), the goal saves
     automatically
   - Previously edits were lost when clicking away — only Enter
     or Ctrl+Enter would save

3. Goal checkmark circle too big:
   - Separated the tap target (36px button) from the visual circle
     (20px inner span with border)
   - Previously the button itself was the circle with minHeight: 44
     making it 44px — visually too large
   - Now the button is a transparent 36px tap target wrapping a
     20px circle, keeping accessibility while looking compact

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
