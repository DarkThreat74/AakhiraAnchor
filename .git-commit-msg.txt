Fix Today animations, habit checkboxes, drag reordering, charts, compass

- Today goal animation: include done goals during 800ms exit animation window
  so strike-through + fade plays before removal (was filtered out instantly)
- Today habit checkboxes: make habit dots clickable buttons that toggle
  habit-logs API + optimistic state (was display-only div with no onClick)
- Goal reordering: add GripVertical drag handle to GoalsTab list view,
  persist sortOrder via PATCH /api/goals (already supported server-side)
- Habit reordering: add drag handle to HabitsTab, persist via PATCH /api/habits
  (added sortOrder support to habits PATCH route)
- Day-of-Week Consistency chart: fix >100% values by counting totalPrayed
  and activeDays from the SAME date range (was all-time vs range mismatch)
- Clamp perPrayer consistencyPct to 100
- Qibla compass: register BOTH deviceorientationabsolute and deviceorientation
  events (was either/or — many Android devices report absolute support but
  never fire the event, leaving compass with zero data)
- Bump SW cache version v29 -> v30

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
