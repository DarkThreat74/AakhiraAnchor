Add talks library, next-prayer countdown, and more landing page features

Continuing the incomplete-feature audit. The talks table existed in the
schema with an admin API but had no user-facing page — now built.

1. Talks Library (/talks)
   - New user-facing GET /api/talks route (auth-protected)
   - New /talks page with:
     - Curated list of external talks (links only, never self-hosted)
     - Category filter pills
     - Speaker and category display
     - External link icon, opens in new tab with rel=noopener
     - Graceful empty state when table not seeded
   - Nav links in desktop sidebar + mobile top bar

2. Next-Prayer Countdown (prayer dashboard)
   - New countdown banner at the top of the "Today" tab
   - Shows the next prayer name and a live countdown (Xh Xm Xs)
   - Handles the cross-midnight case (after Isha, counts to tomorrow's Fajr)
   - Updates every second via the existing currentTime state

3. Landing page updated with talks library feature card.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
