Mobile captcha sizing, Today card wrapper, friends accept/reject, device labels, settings restructure

- Playcaptcha: responsive CSS overrides for 375px and 320px screens so the
  claw machine fits and stays playable on small phones
- Today tab: wrapped all sections in the same rounded-2xl border card used
  by Calendar, Prayer, and Settings pages for visual consistency
- Goals tab nav: replaced the ugly full-screen dropdown with a horizontal
  scrollable pill bar on mobile
- Settings: moved Log out to the very bottom; renamed "Delete Account" to
  "Advanced" with the delete button nested inside
- Trusted devices: parse User-Agent with ua-parser-js to show real device
  names (e.g. "Windows - Chrome", "iPhone - Safari") instead of generic
  "Trusted device"; uses Monitor icon for desktop, Smartphone for mobile
- Prayer friends: added pending/accepted/rejected status flow with
  migration 0012; friend requests now require mutual acceptance before
  either side can see the other's stats; push notifications sent on
  request and acceptance; pending requests UI with accept/reject buttons
  in the Prayer dashboard Friends tab

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
