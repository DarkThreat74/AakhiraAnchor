Homework UX improvements: collapsible classes, confirmations, due times, upcoming in Today

Today tab:
- Replaced "Due today" section with "Upcoming" showing the 5 closest
  pending homework sorted by due date+time
- "More" button expands to show all upcoming; "Show less" collapses
- Each item shows priority dot, title, and relative date badge
  (today/tomorrow/in 3d/etc.)

Homework tab:
- Classes section is now a collapsible dropdown (collapsed by default)
  so homework is the main focus
- Class count badge and active filter chip shown in the dropdown header
- Compact "Add" button inside the dropdown header
- Class rows show color dot, name, pending count, and always-visible
  delete button (works on mobile, no longer hover-only)
- Delete homework now requires confirmation modal
- Completing homework now requires confirmation ("Did you complete it?"
  with "Yes, completed" / "Not yet")
- Uncompleting (re-opening) does not require confirmation
- Due time now shown on cards when set (e.g. "Due at 12:00 PM",
  "Tomorrow at 9:30 AM", "In 3 days at 2:00 PM")
- Overdue items with due time show "2d overdue - Due at 12:00 PM"

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
