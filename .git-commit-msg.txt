Fix goals show-more button, revert to plain text more buttons, fix orange pill colors

1. Goals "show more" was never appearing because shortTermGoals was
   hard-capped at 3 items via .slice(0, 3) before being passed to
   CollapsibleSection. Since items.length === initialLimit === 3,
   hasMore was always false. Removed the slice so all goals are passed
   to the section, and the CollapsibleSection handles the limit + show
   more logic as intended.

2. Reverted all "more" buttons (upcoming homework, goals for this week,
   goals long-term) back to plain text links instead of shaded pills.
   The user wanted normal text, not styled pills.

3. Fixed urgency pill colors — everything was orange because both
   "tomorrow" and "soon" (2-3 days) used var(--color-warmth). Now only
   "tomorrow" uses warmth/amber. "Soon" (2-3 days) and "this_week"
   (4-6 days) both use neutral gray (var(--color-ink-soft) on
   var(--color-paper-2)), same as "later". This means:
   - Overdue: red (bold)
   - Today: red (bold)
   - Tomorrow: amber/orange (semibold)
   - Soon (2-3d): gray
   - This week (4-6d): gray
   - Later: muted gray
   Only genuinely urgent items (overdue/today/tomorrow) get color.

4. Removed the orange card tint for "soon" items since they're no
   longer orange.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
