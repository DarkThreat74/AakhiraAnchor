feat: redesign Akhirah Card, Talks library with R2 audio, fix compass + calendar tint

Bug fixes:
- Calendar: reduce prayer band opacity from 4% to 2% to eliminate bluish tint
- Compass: remove 30ms event dedup that skipped valid readings, fix e.absolute
  check (many Android devices report undefined instead of true), always compute
  heading from alpha/beta/gamma instead of gating on hasAbsoluteRef

Akhirah Card redesign:
- Card number now shows actual total amount in cents (16 digits, padded)
- Cardholder name displays user's displayName from profile
- Card has a HandHeart icon and improved layout
- History redesigned as individual cards with category color dots
- Add button is now a floating FAB with active:scale feedback
- Add form opens as a bottom sheet on mobile, dialog on desktop
- Stats row shows entries, total, and average
- Category breakdown as pills instead of cards

Hijri converter removed:
- Deleted /hijri route and HijriConverterClient
- Removed from Tools menu
- Removed from service worker precache list

Talks library with R2 audio:
- New talk_folders table for organizing talks into folders
- Talks table updated: added storageKey, fileSize, duration, description,
  folderId; externalUrl now nullable for self-hosted talks
- Cloudflare R2 integration via @aws-sdk/client-s3 for MP3 storage
- Presigned PUT URLs for direct client-to-R2 upload (no server file handling)
- Presigned GET URLs for streaming (1 hour expiry)
- Admin portal: create/delete folders, upload MP3 with metadata (title,
  speaker, description), delete talks (also removes from R2)
- User-facing Talks Library redesigned with folder navigation
- Media player with play/pause, skip 15s, seek bar, auto-advance
- Media Session API integration for background playback controls
  (lock screen + notification controls on iOS/Android)
- Save for Offline Use: caches audio via Cache API (waqt-v31-audio)
- Offline indicator on talks that are cached
- Remove offline copy button in player

Migration 0018_talks_folders_r2.sql must be run against Neon.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
