Fix offline caching gaps: series events fallback, goals toggle, list view cache-first, homework optimistic

Offline caching improvements across all recently added features:

Series events list (DayViewClient):
- fetchSeriesEvents now falls back to IndexedDB cache when offline
- Filters cached events by seriesId to show all occurrences even
  without network

Goals toggle (TodayTab):
- Optimistic cache write now happens immediately on toggle, not just
  on API success
- Offline goal toggles are persisted to IndexedDB so they survive
  page reloads
- When API succeeds, cache is refreshed with server-confirmed data

Calendar list view (ListViewClient):
- Now loads from IndexedDB cache first for instant display
- Then fetches from API to get fresh data
- If offline, cached data remains visible (no loading spinner stuck)

Homework complete toggle (HomeworkClient):
- No longer reverts optimistic state on network error
- Offline completions are kept in both UI and cache
- Previously, completing homework while offline would undo the change

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
