Fix desktop Tools button, clean up stale references, update docs

1. Fixed a critical bug: the desktop sidebar Tools button was using
   onClick/window inside a server component, which would fail at runtime.
   Extracted it into the ToolsMenu client component with a variant="sidebar"
   prop so the sidebar renders a proper client-side button.

2. Cleaned up all stale references to removed features:
   - Removed dead "Oath Settings" and "Daily Huddle" section comments
     from src/lib/db/schema.ts
   - Updated the account-deletion cascade comment in
     src/app/api/auth/delete/route.ts to list only existing tables
     (removed onboarding_responses, huddle_completions, daily_lesson_views,
     oath_ledger, oath_settings; added sadaqah_logs)
   - Removed the stale "OpenRouter fallback" comment from
     src/app/api/learn/search/route.ts
   - Rewrote terms/page.tsx section 3 from "oath ledger" to "Akhirah Card"
     and removed "daily lessons" and "AI tone variation" from section 4
   - Removed "daily lessons" from privacy/page.tsx religious content section

3. Updated the landing page (src/app/page.tsx) to reflect the new tools:
   - Renamed "Sadaqah tracker" to "Akhirah Card"
   - Renamed "Hijri calendar" to "Hijri calendar & converter"
   - Added "99 Names of Allah" feature card
   - Updated Dhikr and Qibla copy to mention the redesigns

4. Updated AGENTS.md to remove all dead-feature references:
   - Project description now lists the actual shipped tools
   - Removed "Huddle pool size" and "AI tone variety" from premium examples
   - Removed daily_lessons from the no-AI-content principle
   - Build order: removed step 8 (Voice/AI event entry), merged step 7
     into "Tools (dhikr + qibla + sadaqah + names + hijri + talks)"
   - Directory structure: removed huddle/, lesson/, lessons/, llm.ts;
     added qibla/, sadaqah/, names/, hijri/
   - Removed "AI endpoints: 15/60s" from rate limiting rules
   - Onboarding: removed the religiosity quiz step
   - Content tables: removed daily_lessons and huddle_task_pool
   - External API calls: removed LLM section
   - Open decisions: removed "Daily lesson content bank"
   - Anti-patterns: updated "AI-generate hadith/dhikr/lesson" to
     "AI-generate dhikr/talks"

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
