Fix Isha window end + Asr masjid question timing

Bug 1: Isha "time left" showed time until midnight, not until next Fajr.
The dashboard computed Isha's window end as 24*60 (midnight) instead of
next day's Fajr. Now uses fajrMinutes + 1440, and handles the
after-midnight case with effectiveCurrent (currentMinutes + 1440).

Bug 2: Asr masjid question showed until the last hour of Asr. User wants
30 min before window end (matching Fajr's behavior). Changed from
asrEnd - 60 to asrEnd - 30 in shouldShowMasjidQuestion.

Also added PLAN.md with the Goals section implementation plan.
