Add calendar list view and redesign event color picker

Calendar list view:
- New /calendar/list route showing upcoming events for a full week
- Week navigation (prev/next arrows, week offset tracking)
- Events grouped by day with date card (day name + number)
- Each event shows time range, title, type badge, notify/recurring indicators
- Left border on each event in its color
- Prayer times row shown on the first day of the week
- "Today / Tomorrow / Yesterday" labels on day headers
- Empty state with link to Day view to add events
- Offline support via IndexedDB cache fallback
- Day/Month/List toggle added to Day and Month view headers

Event color picker redesign:
- Replaced large circular buttons with compact rounded-lg grid
- 8 columns on mobile, 12 on desktop
- Selected color shows checkmark icon with drop shadow
- Selected state uses scale(1.1) and outline ring in the color itself
- Color label shown above grid ("Color" + current selection name)
- Fixed duplicate Sage/Olive color (Sage changed to #84a98c)

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
