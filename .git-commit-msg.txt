Today tab: class colors on homework, clickable goal circles, long-term goals; month view homework squares

Today tab - Upcoming homework:
- Each item now shows a left border in the class color
- Class name shown in parentheses in the class color, compact
  (e.g. "Mosquito Pre-lab Quiz (AP Biology)")
- Removed the priority dot in favor of the class color border

Today tab - Goals:
- Short-term goals now limited to max 3 (was 5)
- Short-term and long-term goals use clickable circle checkboxes
  instead of static dots - tap to toggle done/active
- Completed goals show checkmark inside circle, line-through text
- Long-term goals section now always visible (not just when > 0)
  with empty state "No long-term goals yet"
- Default goal color changed from green (var(--color-accent)) to
  var(--color-ink-soft) for short-term and var(--color-ink-muted)
  for long-term, so goals are no longer all green by default
- Goal toggle calls PATCH /api/goals with optimistic update

Month view - Homework indicators:
- Changed from round dots to small squares (rounded-[1px]) to
  differentiate from notification dots
- Moved from bottom of cell to top, next to the day number
- Max 3 squares shown (was 4 dots), with "+N" overflow counter
- Default color changed to var(--color-warmth) when no class color

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
