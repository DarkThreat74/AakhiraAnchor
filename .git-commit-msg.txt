Fix Dhikr counter not showing — use default sequences as fallback

The counter UI was hidden behind an empty state when the
dhikr_sequences table had no rows. Now the component always
renders the counter using built-in default sequences (the canonical
post-prayer adhkar from Sahih al-Bukhari 844 / Sahih Muslim 597)
when the API returns no data. Also reverts the earlier seed commit.

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
