Show event count when applying changes to all events in a series

- Added GET /api/events/bulk?seriesId=... returning the event count
- DayView fetches the count when opening the edit form for a recurring event
- "Apply changes to all events" label now shows the exact count (e.g. "all 57 events")
- "Delete entire series" button also shows the count (e.g. "Delete entire series (57 events)")

Generated with [Devin](https://devin.ai)

Co-Authored-By: Devin <158243242+devin-ai-integration[bot]@users.noreply.github.com>
