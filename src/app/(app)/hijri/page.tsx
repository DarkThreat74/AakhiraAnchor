export const dynamic = "force-dynamic";
export const metadata = { title: "Hijri Converter · Waqt" };

import HijriConverterClient from "./HijriConverterClient";

export default function HijriPage() {
  return <HijriConverterClient />;
}
