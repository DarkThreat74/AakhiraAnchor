import PrayerDashboardClient from "./PrayerDashboardClient";

export const dynamic = "force-dynamic";
export const metadata = { title: "Prayer · Waqt" };

export default function PrayerPage() {
  return <PrayerDashboardClient />;
}
